starlette>=0.40.0
uvicorn[standard]>=0.20.0
openai>=2.0.0
python-dotenv>=1.0.0
requests>=2.28.0
Pillow>=10.0.0
pyOpenSSL>=24.0.0
cryptography>=41.0.0
# MCP 协议支持 (server.py)
mcp>=1.0.0
# 图片 OCR (可选，不装也能用)
# pip install "mineru[core]"
